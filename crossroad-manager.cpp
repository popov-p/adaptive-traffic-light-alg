#include "crossroad-manager.h"

void CrossroadManager::main_loop() {
  while(true) {
   //_active_stage.
  }
};


void CrossroadManager::update_next_stage() {
  auto next_stage = _stages.next(_active_stage);
  //next_stage.get
};
